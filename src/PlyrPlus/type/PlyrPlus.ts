export type VideoChapter = {
    index: number,
    timestamp: string,
    chapterName: string,
    descripiton?: string,
}

export type PlayerStyle = {
    width?: string,
    height?: string,
    maxWidth?: string,
    maxHeight?: string,
    margin?: string,
    padding?: string;
}